name: macOS DMG (Sparkle)
permissions:
  contents: write

on:
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to upload DMG to (e.g., v0.1.2)"
        required: false
        default: v0.1.2

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inizializza TAG_NAME da evento/dispatch (se presente)
        env:
          TAG_EVENT: ${{ github.event.release.tag_name }}
          TAG_INPUT: ${{ inputs.tag_name }}
        run: |
          TAG=""
          if [ -n "$TAG_EVENT" ]; then
            TAG="$TAG_EVENT"
          elif [ -n "$TAG_INPUT" ]; then
            TAG="$TAG_INPUT"
          fi
          if [ -n "$TAG" ]; then
            # Rimuove eventuali spazi non validi
            TAG=$(echo "$TAG" | tr -d '[:space:]')
            echo "TAG_NAME=$TAG" >> "$GITHUB_ENV"
            echo "Preset TAG_NAME=$TAG"
          fi

      - name: Deriva versione da pubspec.yaml
        run: |
          VER_FULL=$(grep '^version:' pubspec.yaml | sed 's/version:\s*//')
          VER_SHORT=${VER_FULL%%+*}
          echo "VERSION_FULL=$VER_FULL" >> $GITHUB_ENV
          echo "VERSION_SHORT=$VER_SHORT" >> $GITHUB_ENV
          # Imposta TAG_NAME solo se non già definito da evento/dispatch
          if ! grep -q '^TAG_NAME=' "$GITHUB_ENV"; then
            TAG=$(echo "v$VER_SHORT" | tr -d '[:space:]')
            echo "TAG_NAME=$TAG" >> "$GITHUB_ENV"
          fi

      - name: Valida TAG_NAME
        run: |
          TAG="$TAG_NAME"
          echo "TAG_NAME=$TAG"
          if [ -z "$TAG" ]; then echo "TAG_NAME vuoto"; exit 1; fi
          if echo "$TAG" | grep -q '[[:space:]]'; then echo "TAG_NAME contiene spazi: '$TAG'"; exit 1; fi
          if ! echo "$TAG" | grep -E '^[A-Za-z0-9._-]+$' >/dev/null; then echo "TAG_NAME non valido: '$TAG'"; exit 1; fi
          echo "TAG_NAME valido"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.7'
          channel: 'stable'

      - name: Enable macOS desktop
        run: flutter config --enable-macos-desktop

      - name: Pub get
        run: flutter pub get

      - name: Optional secrets check (non-blocking)
        run: |
          if [ -z "${{ secrets.SUPABASE_URL }}" ]; then echo "Warning: SUPABASE_URL not set; app will show config error screen at runtime."; fi
          if [ -z "${{ secrets.SUPABASE_ANON_KEY }}" ]; then echo "Warning: SUPABASE_ANON_KEY not set; app will show config error screen at runtime."; fi
          echo "Continuing build regardless of secrets."

      - name: Build macOS Release
        run: flutter build macos --release --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }} --dart-define=APPCAST_URL=${{ secrets.APPCAST_URL }}

      - name: Install create-dmg
        run: |
          brew update
          brew install create-dmg

      - name: Create DMG
        run: |
          TAG_NAME="${{ env.TAG_NAME }}"
          VERSION="${{ env.VERSION_SHORT }}"

          # Detect built .app dynamically
          APP_PATH=$(ls -d build/macos/Build/Products/Release/*.app 2>/dev/null | head -n 1)
          OUT_DIR="dist"
          mkdir -p "$OUT_DIR"
          DMG_NAME="Earon-$VERSION.dmg"

          echo "TAG_NAME=$TAG_NAME"
          echo "VERSION=$VERSION"
          echo "APP_PATH=$APP_PATH"
          echo "DMG_NAME=$DMG_NAME"

          if [ -z "$APP_PATH" ] || [ ! -d "$APP_PATH" ]; then
            echo "ERROR: No .app found under build/macos/Build/Products/Release/"
            ls -la build/macos/Build/Products/Release/ || true
            exit 1
          fi

          # Create DMG (remove existing file; Homebrew create-dmg does not support --overwrite)
          rm -f "$OUT_DIR/$DMG_NAME"
          create-dmg --volname "Earon" "$OUT_DIR/$DMG_NAME" "$APP_PATH"
          echo "DMG created at $OUT_DIR/$DMG_NAME"

      - name: Crea release GitHub e carica DMG (tag derivato)
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/Earon-*.dmg
          tag_name: ${{ env.TAG_NAME }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Nota: l'aggiornamento di updates/appcast.xml è gestito da un workflow dedicato post-release per evitare conflitti con le regole di protezione del branch.