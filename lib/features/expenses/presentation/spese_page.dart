import 'package:flutter/material.dart';

class SpesePage extends StatelessWidget {
  const SpesePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Spese (stub)'));
  }
}
