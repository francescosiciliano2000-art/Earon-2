import 'package:flutter/material.dart';

class FatturePage extends StatelessWidget {
  const FatturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Fatture (stub)'));
  }
}
