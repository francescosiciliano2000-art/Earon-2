import 'package:flutter/material.dart';

class NotifichePage extends StatelessWidget {
  const NotifichePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Notifiche (stub)'));
  }
}
